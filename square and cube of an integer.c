#include <stdio.h> 

int main() 
{ 
    int number; 
    printf("\nEnter an integer: "); 
    scanf("%d", &number); 
    printf("\nSquare of the number is %d", number * number); 
    printf("\nCube of the number is %d", number * number * number); 
    return 0; 
}
